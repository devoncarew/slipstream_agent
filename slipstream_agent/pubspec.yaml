name: slipstream_agent
description: In-process companion for Slipstream Flutter agent tools.
version: 0.1.0

environment:
  sdk: ^3.3.0
  flutter: ">=3.38.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
